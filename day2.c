#include <stdio.h>

int main(){
    int a,b;
    printf("enter the value of the number 1\n");
    scanf("%d", &a);
    
    printf("enter the value of the second number\n");
    scanf("%d", &b);

    int sum;
    sum == a+b;


    int diff;
    diff == a-b;


    int mul;
    mul == a*b;


    int ques;
    ques == a+b;
    
    printf("the sum of the two number is %d", sum);
    printf("the sum of the two number is %d", diff);
    printf("the sum of the two number is %d", mul);
    printf("the sum of the two number is %d", ques);

}